name: cli_menu
description: >
  A simple menu (picker) that works on the command line. Works on Linux,
  Mac and Windows. Supports choosing by arrows on ANSI terminals.
version: 0.4.0
homepage: https://github.com/filiph/cli_menu

environment:
  sdk: ^3.0.0

dependencies:
  cli_util: ^0.4.0

dev_dependencies:
  lints: ^3.0.0
