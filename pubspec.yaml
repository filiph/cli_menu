name: cli_menu
description: >
  A simple command-line menu (picker) compatible with Linux,
  Mac and Windows. It supports selection by using arrow keys
  on ANSI terminals.
version: 0.4.0+1
homepage: https://github.com/filiph/cli_menu

environment:
  sdk: ^3.0.0

dependencies:
  cli_util: ^0.4.0

dev_dependencies:
  lints: ^3.0.0
